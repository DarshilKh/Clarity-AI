[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# All API routes are automatically handled as Netlify Functions
# via @netlify/plugin-nextjs — no extra config needed
